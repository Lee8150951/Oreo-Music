import React from 'react';
import '../style/views/Setting.scss';

interface Props {
  children?: React.ReactNode;
}

const Setting: React.FC<Props> = (props): JSX.Element => {
  /** state **/

  /** effect **/

  /** methods **/

  /** render **/
  return (
    <div>
      <div>setting</div>
    </div>
  );
};

export default Setting;
